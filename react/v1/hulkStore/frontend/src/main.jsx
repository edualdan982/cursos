import React from 'react';
import ReactDOM from 'react-dom/client';
import { HulkStoreApp } from './HulkStoreApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <HulkStoreApp />
  </React.StrictMode>,
)
